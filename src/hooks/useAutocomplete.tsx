import { useMemo } from "react";
// import { FIELDS, TOKYO_BOUNDS } from "../constants";

type useAutocompleteProps = {
  googleMap: typeof google;
  inputEl: HTMLInputElement;
};

//TODO: unbind

// export const useAutocomplete = ({ googleMap, inputEl }: useAutocompleteProps) =>
//   useMemo(, []);
